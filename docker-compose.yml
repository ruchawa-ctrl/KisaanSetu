services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: kisaan_setu
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports: ["5432:5432"]
    volumes: [kisaan-setu-postgres:/var/lib/postgresql/data]
volumes:
  kisaan-setu-postgres:
